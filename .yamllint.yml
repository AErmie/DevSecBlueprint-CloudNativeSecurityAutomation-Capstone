---
extends: default

rules:
  new-lines: disable
  truthy: disable
  line-length:
    max: 120
    level: warning
  indentation:
    spaces: 2
    indent-sequences: consistent
  trailing-spaces: enable
  document-start: disable
